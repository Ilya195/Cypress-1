{
  "name": "cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cypress open --config-file",
    "test:cypress": "cypress open --browser chrome",
    "test:allHeaded": "cypress run --browser chrome --headed --config-file cypress.desktop.env.js",
    "test:allHeadless": "npx cypress run --headless -b chrome",
    "test:allHeadedChrome": "npx cypress run --browser chrome --headed --config-file cypress.mobile.env.js",
    "test:spec": "cypress run --headed --browser chrome --spec cypress/e2e/login.cy.js"
  },
  
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^10.11.0",
    "jest": "^29.5.0"
  }
}
